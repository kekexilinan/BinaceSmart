- 对外部模块使用动态 `import()` 按需加载，仅在需要时才引入对应函数，减少启动开销。
- 飞书卡片推送统一通过内部 `sendFeishuCard(title, elements, template, webhook)` 封装，复用相同的 JSON body 结构与 `fetch` 调用。
- 环境变量通过本地 `loadEnv()` 解析 `.env` 后以 `process.env.XXX` 形式访问，未提供则回退到硬编码默认值（如 `0.25`、`15`）。
- 合成数据通过浅拷贝 `row` 并追加 `whaleRatio/globalRatio/divergence/change24h` 字段注入，保持原始数据结构不变。