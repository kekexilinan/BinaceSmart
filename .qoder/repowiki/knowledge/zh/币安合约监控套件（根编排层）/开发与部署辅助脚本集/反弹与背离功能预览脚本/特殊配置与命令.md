运行前需在仓库根 `.env` 中设置 `FEISHU_WEBHOOK`（必填），可选配置 `SMART_TREND_DIVERGENCE_THRESHOLD`、`REBOUND_HIGHLIGHT_CHANGE_PCT`、`SMART_TREND_MIN_RANKING_VOLUME_24H`；同时需保证 `data/smart-trend-push-mock.json` 存在。执行命令：`node scripts/preview-rebound-features.mjs`。