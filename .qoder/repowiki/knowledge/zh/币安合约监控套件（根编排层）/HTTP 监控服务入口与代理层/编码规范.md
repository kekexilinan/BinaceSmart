- 对外部 HTTP 调用统一走 `proxy-setup.mjs` 导出的 `fetchJson(url, { timeoutMs, preferCurl })`，禁止直接使用全局 `fetch`。
- 所有 Binance FAPI 请求经 `proxyBinance(path, { retries })` 包装，带指数退避重试与错误信息本地化。
- 定时任务采用 `runXxxRunning` 布尔锁 + `finally` 复位模式，防止同一任务并发执行。
- 配置项一律从 `process.env` 读取并附带默认值，推送开关遵循 `xxx_ENABLED !== 'false' && !!WEBHOOK` 的双检模式。
- 飞书推送使用 `sendFeishuCardV2(title, elements, template)` 构建 schema 2.0 卡片，并对频控错误做 4 次重试。
- 币种过滤链式调用 `filterTradFiItems` → `filterItemsByMarketCap`，统一通过 `filterEligibleSymbols(items, key)` 入口。