- 对外 API 采用「异步加载 + 同步判断」分离：数据获取函数返回 Promise，查询函数直接读取模块级 cache Set。
- 所有输入 symbol 统一先 `.toUpperCase()` 再匹配，避免大小写不一致导致的误判。
- 基础资产提取使用链式正则替换去除 USDT/BUSD/USDC 后缀，而非硬编码白名单。
- 可选功能通过顶层 `process.env.* === 'true'` 布尔常量导出作为全局开关，由调用方决定是否走过滤逻辑。